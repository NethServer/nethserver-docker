#
# 50aqua -- masquerade the green network if red does not exist
#
{
    use esmith::NetworksDB;
    my $ndb = esmith::NetworksDB->open_ro();
    
    $OUT = '';
    if( ! $ndb->red) {
        foreach ($ndb->green) {
            $OUT .= $_->key . "\n";
        }
    }
}

